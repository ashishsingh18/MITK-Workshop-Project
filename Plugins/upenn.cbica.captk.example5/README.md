## upenn.cbica.captk.example5

Example plugin targeted to developers to demonstrate usage / guidelines.

This demonstrates a plugin that is based on module "example5" which internally uses python for computation.